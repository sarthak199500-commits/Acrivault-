node_modules
dist
dist-ssr
coverage
*.local
.vite

# TypeScript incremental build cache. Machine-local and rewritten on every
# build, so tracking it left every worktree permanently dirty.
*.tsbuildinfo

# design-sync: staged scripts, build output, and machine-local state.
# Everything else under .design-sync/ (config.json, NOTES.md, conventions.md,
# previews/, overrides/) is durable and committed.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
